import { MessageMagicDirective } from './message-magic.directive';

describe('MessageMagicDirective', () => {
  it('should create an instance', () => {
    const directive = new MessageMagicDirective();
    expect(directive).toBeTruthy();
  });
});
